import 'package:flutter/material.dart';

class VolumeOneChapters extends StatelessWidget {
  const VolumeOneChapters({Key? key, required this.index}) : super(key: key);

  final int index;

  @override
  Widget build(BuildContext context) {
    return ListView();
  }
}
